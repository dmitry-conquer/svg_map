type TypeAccordioState = {
  activeAccordionIndex: number;
};

type TypeTabsState = {
  activeTabIndex: number;
};

type Marker = {
  x: number;
  y: number;
  content: string;
  imageSrc: string;
};
