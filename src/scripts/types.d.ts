type TypeAccordioState = {
  activeAccordionIndex: number;
};

type TypeTabsState = {
  activeTabIndex: number;
};

type MarkerData = {
  id?: number;
  x: number;
  y: number;
  content: string;
  imageSrc: string;
  area: string;
};
