type TypeAccordioState = {
  activeAccordionIndex: number;
};

type TypeTabsState = {
  activeTabIndex: number;
};

type Marker = {
  id?: number;
  x: number;
  y: number;
  content: string;
  imageSrc: string;
};
